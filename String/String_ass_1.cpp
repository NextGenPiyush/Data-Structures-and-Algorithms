#include<bits/stdc++.h>
using namespace std;



int main(){

//*********************************************************************************************************************//

    // string str;
    // getline(cin,str);
    // cout<<str<<endl;
    // for(int i=1; i<str.size(); i+=2) str[i] = '#'; 
    // cout<<str;

//*********************************************************************************************************************//

    // string str;
    // getline(cin,str);
    // int count = 0;
    // for(int i=0; i<str.size(); i++) if(str[i]!='a' && str[i]!='e' && str[i]!='i' && str[i]!='o' && str[i]!='u') count++;
    // cout<<"Number of consonants: "<<count<<endl;

//*********************************************************************************************************************//

    // string str;
    // getline(cin,str);
    // int left = 0, right = str.size()-1;
    // bool flag = true;
    // while(left < right) if(str[left++] != str[right--]) flag = false;
    // if(flag) cout<<"String is a palindrome"<<endl;
    // else cout<<"String is not a palindrome"<<endl;

//*********************************************************************************************************************//

    // string str;
    // getline(cin,str);
    // cout<<str<<endl;
    // int n = str.size();
    // reverse(str.begin()+(n/2),str.end());
    // cout<<str<<endl;

//*********************************************************************************************************************//

    // string str;
    // cin>>str;
    // int num = 0;
    // for(int i=0; i<str.size(); i++){
    //     int new_num = str[i]-'0';
    //     num = num*10+new_num;
    // }
    // cout<<"Number will: "<<num<<endl;

//*********************************************************************************************************************//


    return 0;
}