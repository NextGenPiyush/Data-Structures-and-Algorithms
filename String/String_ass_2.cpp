#include<bits/stdc++.h>
using namespace std;



int main(){

//*********************************************************************************************************************//

    // string str,revstr;
    // cin>>str;
    // for(int i=str.size()-1; i>=0; i--) revstr.push_back(str[i]);
    // str.append(revstr);  // we can also do this using reverse function after copying string str into a new one 
    // cout<<str;

//*********************************************************************************************************************//

    // string str;
    // cin>>str;
    // int largest = INT_MIN;
    // int second_largest = INT_MIN;
    // for(int i=0; i<str.size(); i++){
    //     int num = str[i]-'0';
    //     if(largest < num){
    //         second_largest = largest;
    //         largest = num;
    //     }
    //     else if(second_largest < num && second_largest != largest) second_largest = num;
    // }
    // if(largest != INT_MIN) cout<<"largest: "<<largest<<endl;
    // else cout<<-1<<endl;
    // if(second_largest != INT_MIN) cout<<"second_largest: "<<second_largest<<endl;
    // else cout<<-1<<endl;

//*********************************************************************************************************************//

         

//*********************************************************************************************************************//

    // string s1,s2;
    // getline(cin,s1);
    // getline(cin,s2);
    // sort(s1.begin(),s1.end());
    // sort(s2.begin(),s2.end());
    // if(s1 == s2) cout<<"strings are anagram"<<endl;
    // else cout<<"strings are not anagram"<<endl;

//*********************************************************************************************************************//

    // string str,temp;
    // getline(cin,str);
    // stringstream ss(str);
    // vector<string> v;
    // while(ss>>temp) v.push_back(temp);
    // sort(v.begin(),v.end());
    // cout<<v.back()<<endl;

//*********************************************************************************************************************//



    return 0;
}