#include<bits/stdc++.h>
using namespace std;



int main(){

    // you are starting at (0,0) and want to reach (m-1,n-1) 
    // you have to return if it is possible or not

    // we can do this problem with some sapce complexity by bfs,dfs, recursion... but constarint is space complexity:O(1) 
    // hint: use slow and fast pointer approach as we  have done in linkedList
    // hint: follow the path for maximum m*n times if not get to the end then return vcz it is only possible when 
    // you get stuck into cycle

    return 0;
}