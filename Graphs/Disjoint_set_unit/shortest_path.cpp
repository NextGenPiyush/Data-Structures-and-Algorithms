#include<bits/stdc++.h>
using namespace std;



int main(){

    // Question-1: you are given an undirected and unweighted graph with a source and destination node.
    // every edge has a color either red or blue. Find the shortest path between source and destination such that:
    // 1.> The path always start at red edge and ends at blue edge.
    // 2.> you can shift from red edge to blue only ones.
    // 3.> there are no self loop or multiple edges
    
    // Question-2: given an undirected and unweighted graph with source and destination. Find all nodes which come 
    // in atleast one shortest path from source to destination

    

    return 0;
}