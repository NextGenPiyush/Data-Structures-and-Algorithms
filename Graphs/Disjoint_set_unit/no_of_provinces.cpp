#include<bits/stdc++.h>
using namespace std;



int main(){

    // one method is that we call bfs/dfs and count how many time we can do bfs/dfs  (connected components)
    // another way of doing this by using DSU  
    // do it yourself     

    // how to construct graph --> reffer lecture 9

    return 0;
}